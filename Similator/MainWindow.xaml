<Window x:Class="Similator.MainWindow"
        
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Similator"
        mc:Ignorable="d"
        Title="Wafer Handling HMI" Height="450" Width="800">
    <Grid>

 
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Header -->
                    <RowDefinition Height="*"/>
                    <!-- Main Content -->
                    <RowDefinition Height="Auto"/>
                    <!-- Footer / Controls -->
                </Grid.RowDefinitions>

                <!-- Header -->
                <TextBlock Text="Wafer Handling System HMI"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="10"/>

                <!-- Main Content -->
                <Grid Grid.Row="1" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Load Port 1 -->
          

                    <!-- Load Port 2 -->
                   
                </Grid>

                <!-- Footer Controls -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                    <Button Content="Start" Command="{Binding StartCommand}" Width="100" Margin="5"/>
                    <Button Content="Pause" Command="{Binding PauseCommand}" Width="100" Margin="5"/>
                </StackPanel>
            </Grid>
       



    </Grid>
</Window>
